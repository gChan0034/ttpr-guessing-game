# Number Guessing Game Assignment

## Requirements
1. Complete all empty functions/methods
2. Use proper array methods and DOM manipulation
3. Handle edge cases (invalid input, duplicates)
4. Display game state visually
5. Add event listeners for user interaction

## Implementation Steps
1. **Core Logic**
   - Implement `generateWinningNumber`
   - Complete `shuffle` using Fisher-Yates
   - Fill in `Game` class methods

2. **DOM Interaction**
   - Set up event listeners
   - Update display for guesses
   - Show feedback messages
   - Handle game reset

3. **Validation**
   - Prevent non-numeric input
   - Handle out-of-range numbers
   - Track previous guesses

## Hints
- Use `Math.random` for number generation
- Remember array indices start at 0
- Use `classList` for CSS updates
- Test boundary conditions (1, 100, 5th guess)

## Testing
Run `testem` to validate your implementation against the specs